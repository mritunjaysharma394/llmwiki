# Mini Repo

A tiny test fixture used by the `TestIngestRepo` cassette test. It exists so
the directory walker has something realistic to walk: a Markdown README, a Go
source file, a `go.mod`, plus a `node_modules` directory the walker is
expected to skip.
